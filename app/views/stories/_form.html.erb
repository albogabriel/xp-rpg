<%= form_for(@story) do |f| %>
  <% if @story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
      <% @story.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div> -->
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :estimate %><br />
    <%= f.text_field :estimate %>
  </div>
  <div class="field">
<label for="businessValue">Business Value<br> 
    <%= f.text_field :businessValue %></label>
  </div>

    <div class="field">
	Iteration
	  <% iterations = Iteration.find(:all, :order => :id) %>
	  <%= f.collection_select :iteration_id, iterations, :id, :label %>
   </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
